name: deploy docker image to ec2

on: 
    push

jobs:
    build:
        runs-on: self-hosted

        steps:
          - name: code check
            uses: actions/checkout@v3 

          #- name: Login to Docker Hub
           # uses: docker/login-action@v2
            #with:
             # username: ${{ secrets.DOCKER_USERNAME
              #password: ${{ secret

         ## - name: Set up Docker Buildx
           # uses: docker/setup-buildx-action@v1

          #- name: Build and Push Docker Image
          #  env:
           #   DOCKERHUB_USERNAME: ${{ secrets.DOCKER_USERNAME }
            #  DOCKERHUB_ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }
           # run: |
            #  echo "$DOCKERHUB_ACCESS_TOKEN" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
             # docker buildx create --use
              #docker buildx build --platform linux/amd64,linux/arm64 -t $DOCKERHUB_USERNAME/your-image-name:latest . --push
                    
          - name: Configure AWS Credentials
            run: |
             aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/y6b5h0d4
             docker build -t my_repo .
             docker tag my_repo:latest public.ecr.aws/y6b5h0d4/my_repo:latest
             docker push public.ecr.aws/y6b5h0d4/my_repo:latest